dist: bionic
language: python

python:
  - '3.8'

addons:
  chrome: stable

branches:
  only:
    - main

install:
  - pip install pipenv
  - pipenv install --dev

services:
  - postgresql

script: pytest